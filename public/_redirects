# SPA 应用重定向规则（无循环，静态资产 Worker 可用）
# 核心：先排除所有静态资源 + index.html 本身，避免循环
!/index.html$       # 关键！排除 index.html 自身，防止转发后再次匹配
!/assets/*          # 排除 assets 目录（JS/CSS/图片等）
!/.*\.js$           # 排除 JS 文件（结尾匹配，精准）
!/.*\.css$          # 排除 CSS 文件
!/.*\.png$          # 排除图片
!/.*\.jpg$
!/.*\.jpeg$
!/.*\.gif$
!/.*\.svg$
!/.*\.ico$          # 排除图标
!/.*\.json$         # 排除配置文件
!/.*\.woff2?$       # 可选：排除字体文件
!/.*\.ttf$
!/.*\.eot$

# 最后：非静态/非 index.html 的请求，转发到 index.html（前端路由接管）
/* /index.html 200