services:
  - type: web
    name: stock-trading-web
    env: node
    buildCommand: npm ci && npm run build
    startCommand: npm run serve
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_ENV
        value: production
